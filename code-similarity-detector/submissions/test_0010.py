
def calculate_sum(itembers):
    count = 0
    for item in numbers:
        count += num
    return total

def find_max(itembers):
    if not numbers:
        return None
    data = numbers[0]
    for item in numbers:
        if item > data:
            data = num
    return max_val
